<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form px-10 py-5">
        <form action="/create" method="post">
          <input class="block w-full bg-zinc-800 text-white rounded-md
          outline-none px-5 py-2 type="text" placeholder="Title here"
          name="title"/>
          <textarea
            class="block w-full outline-none bg-zinc-800 text-white px-5 py-2 rounded-md resize-none mt-2"
            placeholder="write your task here"
            name="details"
          ></textarea>
          <input
            class="mt-2 bg-blue-600 px-10 py-3 rounded-md"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="task flex gap-3 flex-wrap p-10">
        <% if(files.length > 0) { %> <% files.forEach((file)=>{ %>
        <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white text-3xl tracking-tighter"><%= file%></h1>
          <div class="flex w-full justify-between item-center mt-3">
            <a href="/file/<%= file %>" class="text-blue-600 inline-block"
              >Read More</a
            >
            <a href="/edit/<%=file%>" class="text-zing-600">edit fileName</a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <h1 class="bg-zinc-900">No task yet..</h1>
        <% } %>
      </div>
    </div>
  </body>
</html>
